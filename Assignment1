package assignment1;
import java.lang.Math;//adds the math library
import java.util.Scanner;//adds the scanner library
/**
 *
 * @author Duncan 957353
 */
public class Assignment1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
        Scanner sc = new Scanner(System.in);//adds the scanner
        int length;//declares length as an int
        int width;//declares width as an int
        System.out.print("input length of rectangle: ");//asks what the length is
        length = sc.nextInt();//assigns their input as length
        System.out.print("input width of rectangle: ");//asks what the width is
        width = sc.nextInt();//assigns their input as width
        System.out.println(length*width);//provides the area of the rectangle
        
        int radius;//declares radius as an int
        System.out.print("input radius of circle: ");//asks the radius of the circle
        radius = sc.nextInt();//assigns their input as the radius
        System.out.println(Math.PI*(radius*radius));//provides the area of the circle
        
        int CR;//declares the cylinder radius as an int
        int H;//declares the cylinder height as an int
        System.out.print("input radius of cylinder: ");//asks the radius of the cylinder
        CR = sc.nextInt();//assigns their input as the cylinder radius
        System.out.print("input height of cylinder: ");//asks the height of the cylinder
        H = sc.nextInt();//assigns their input as the height
        System.out.println(Math.PI*(cradius*CR)*H);//provides the volume of the cylinder
        System.out.println((2*Math.PI)*CR*H +(2*Math.PI)*CR*2);
        
        int L;
        double G;
        G = 9.8;
        System.out.print("input meters: ");
        L = sc.nextInt();
        System.out.print((Math.PI*Math.PI)Math.sqrt(L/G));
    }
    
}
